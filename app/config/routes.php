<?php

// default controller if no URI is specified
$map[''] = 'example';

// add any additional routes with the route as the key and controller as the value
// routes will be checked before controller paths, so a controller path can certainly be overridden here
// map['quick/route/(.*)'] = 'super/long/route/to/controller/$1';

?>